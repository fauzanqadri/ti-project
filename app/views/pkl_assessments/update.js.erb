var a = $("#pkl-assessments").dataTable();
a._fnAjaxUpdate();
$("#pkl-assessment-modal").modal('hide');
$("#pkl-assessment-modal").on('hidden.bs.modal', function(e){
	$(e.target).remove();
	$("#the-flash").append("<%= j render partial: 'layouts/shared/flash', locals: {flash: flash} %>");
	$("#the-flash .alert").each(function(i, e){
		$(this).delay(i*1000).fadeIn("slow");
	});
});