var html = "<%= j render('show') %>";
var sidangModal = $("#sidang-modal");
if (sidangModal.length == 1) {
	sidangModal.modal("hide");
	sidangModal.on('hidden.bs.modal', function(e){
		$(e.target).remove();
		$("body").append(html);
		$("#sidang-modal").modal({show: true});
	});
}else{
	$("body").append(html);
	$("#sidang-modal").modal({show: true});	
}