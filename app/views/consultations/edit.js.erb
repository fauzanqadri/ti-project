var html = "<%= j render('form') %>";
var lecturerModal = $("#consultation-modal");
if (lecturerModal.length == 1) {
	lecturerModal.modal("hide");
	lecturerModal.on('hidden.bs.modal', function(e){
		$(e.target).remove();
		$("body").append(html);
		$("#consultation-modal").modal({show: true});
	});
}else{
	$("body").append(html);
	$("#consultation-modal").modal({show: true});	
}
$(".datepicker").datetimepicker({
	language: 'id',
	icons: {
		time: "fa fa-clock-o",
        date: "fa fa-calendar",
        up: "fa fa-arrow-up",
        down: "fa fa-arrow-down"
    }
});